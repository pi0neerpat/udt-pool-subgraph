{
  "name": "udt-eth-pool",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create pi0neerpat/udt-eth-pool --node http://127.0.0.1:8020",
    "deploy-local": "graph deploy pi0neerpat/udt-eth-pool --ipfs http://localhost:5001 --node http://127.0.0.1:8020",
    "deploy": "graph deploy pi0neerpat/udt-eth-pool --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.18.0",
    "@graphprotocol/graph-ts": "^0.18.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
